I'll read the suggested edit carefully and apply it to update the session start prompt file.

The suggested edit is a **complete replacement** of the `00-SESSION-START-PROMPT.md` file (formerly the inline prompt). Here's what changed:

**Key changes in the new version:**

1. References `v1` branch (not `master`) as source of truth
2. Adds a 4th doc file (`00-SESSION-START-PROMPT.md` itself)
3. Adds WT v1 key design token values table
4. Adds current build status (✅ done / ❌ not yet built)
5. Adds explicit session rules
6. Adds example session prompts
7. Updates all `git show` commands to use `wt/v1:` prefix

Here is the complete updated file:
